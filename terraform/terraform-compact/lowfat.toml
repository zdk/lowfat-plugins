[plugin]
name = "terraform-compact"
version = "0.6.0"
description = "Compact terraform/tf output for LLM contexts"
author = "zdk"
commands = ["terraform", "tf"]
subcommands = ["plan", "apply", "destroy", "init", "validate", "fmt", "state", "output"]
bin = "terraform"

[runtime]
entry = "filter.lf"
