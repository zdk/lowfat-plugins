[plugin]
name = "pytest-compact"
version = "0.1.1"
description = "Compact pytest output for LLM contexts (failures-only at ultra)"
author = "zdk"
commands = ["pytest", "py.test"]
subcommands = []
bin = "pytest"

[runtime]
entry = "filter.lf"
